# Release Workflow

1. **Verify Version:** Check `package.json` version number.
2. **Execute Full Verification:** Run `npm run check` to ensure there are no linter/type errors.
3. **Compile Release Bundle:** Execute `npm run build` to compile the standalone Next.js build.
4. **Draft Release Notes:** Document changes, index creations, caching configurations, and rate-limiting updates.
