# Directory & Path Ignored Constraints

- Never commit `.env` configuration files.
- Ignore Next.js compiled directories: `.next/`, `out/`, `build/`.
- Ignore Node modules: `node_modules/`.
- Ignore package lock files modifications unless new scripts are added.
